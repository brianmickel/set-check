# Server
PORT=3000

# CORS: comma-separated list of allowed origins (optional; defaults include localhost and GitHub Pages)
# FRONTEND_ORIGIN=https://example.com

# OpenAI (if this backend ever calls the API; primary API is in worker/)
# OPENAI_API_KEY=sk-...
